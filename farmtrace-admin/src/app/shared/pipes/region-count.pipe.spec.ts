import { RegionCountPipe } from './region-count.pipe';

describe('RegionCountPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionCountPipe();
    expect(pipe).toBeTruthy();
  });
});
